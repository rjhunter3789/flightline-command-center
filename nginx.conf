# Nginx configuration for Flightline
# Add this to your DigitalOcean server's nginx sites-available directory

server {
    listen 80;
    server_name flightline.yourdomain.com;  # Replace with your domain

    # Frontend
    root /var/www/flightline/frontend;
    index index.html;

    # Frontend routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Backend API proxy
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket support for real-time updates
    location /socket.io {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://localhost:3001/health;
    }
}

# SSL Configuration (after setting up Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name flightline.yourdomain.com;
#     
#     ssl_certificate /etc/letsencrypt/live/flightline.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flightline.yourdomain.com/privkey.pem;
#     
#     # ... rest of configuration same as above
# }